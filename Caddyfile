# Regulayer Caddyfile - Automatic HTTPS & Reverse Proxy

# Route API Gateway Traffic
api.{$REGULAYER_DOMAIN} {
    # CORS — handled here at the outermost layer so every response
    # (including error pages) gets correct headers. No downstream
    # service should add its own Access-Control-* headers.
    @cors_preflight method OPTIONS
    handle @cors_preflight {
        header Access-Control-Allow-Origin "https://{$REGULAYER_DOMAIN}"
        header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS"
        header Access-Control-Allow-Headers "*"
        header Access-Control-Max-Age "600"
        respond "" 204
    }

    handle {
        reverse_proxy gateway:8080
        header Access-Control-Allow-Origin "https://{$REGULAYER_DOMAIN}"
        header Access-Control-Expose-Headers "*"
    }
}

# Route Frontend Web Traffic
{$REGULAYER_DOMAIN} {
    reverse_proxy web:3000
}
